from django.apps import AppConfig


class ContractualisationConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.contractualisation"
    verbose_name = "Contractualisation (NOTI5)"
