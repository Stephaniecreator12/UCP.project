const fetch = require('node-fetch');

async function test() {
  // get token somehow, or just mock? I can't easily do it without a valid session.
  console.log("No token available to easily test API directly.");
}
test();
