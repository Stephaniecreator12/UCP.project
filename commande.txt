#environnement virtuelle
creer: python -m venv venv
activer: venv\Scripts\activate

#Pour lancer le server
python manage.py runserver

#Pour updater le plan d'actions
python manage.py makemigrations marches

#Pour migrer dans la Bdd
python manage.py migrate

#SQL SHELL
Server [localhost]: Appuie sur Entrée (sauf si ta base est sur un serveur distant)
Database [postgres]: Tape le nom de ta base de données (celui que tu as défini dans le settings.py de Django)
Port [5432]: Appuie sur Entrée
\l : affiche liste de tout les bdd
\c nomdelabase : se connecter a une base
\dt : affiche liste des tables de la bdd actuelle
SELECT * FROM nom_de_la_table LIMIT 10; : consulter données
\q : quitter

#CREER TOKEN
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

#ENREGISTRE MON TRAVAIL DANS UNE BRANCHE: git add .
git commit -m "commentaire"
git push origin nom_de_ma_branche

#PRENDRE LE CODE DE LA BRANCHE DE QQLQ
git switch ma-branche
git fetch origin : recupere le code de la branche de qqlq
git reset --hard origin/Stephanie

#SOUS DOSSIER DANS MAIN
git checkout ma-branche
git checkout main -- config_app
git commit -m "Récupération du sous-dossier X depuis la branche main"

#CODER SUR 2 BRANCHES DIFFERENTS EN MEME TEMPS
git worktree add ../mon-projet-comparaison nom-de-la-branche
Ouvre une nouvelle fenêtre VS Code (Fichier > Nouvelle fenêtre)
Ouvre le dossier ../mon-projet-comparaison qui vient d'être créé.


>> .get() est ce qu'on appelle une sécurité avec valeur par défaut. <<

#supprimer données dans la table dans sql shell
>> TRUNCATE TABLE nom_de_ta_table RESTART IDENTITY CASCADE; <<
RESTART IDENTITY : Remet les compteurs (ID auto-incrémentés) à zéro.
CASCADE : Supprime aussi les données dans les tables liées par des clés étrangères.
--ou bien sur le terminal -- 
dropdb nom_de_ma_bdd
createdb nom_de_ma_bdd

# TUER LE PROCESSUS NODE(NEXT MI-RUN EN ARRIER PLAN) A LA MAIN
taskkill /F /IM node.exe

#ACTUALISER LE CODE DE MA BRANCHE
git pull origin nom_de_ma_branche

# Stashing is like putting your current work in a temporary drawer. It clears your workspace so you can switch branches, and you can "pop" the drawer(tiroir) open later to get your work back.
To save your work: git stash

# au lieu de install corsheaders, ...
pip install -r requirements.txt